import { NextFunction, Request, Response } from "express";

export const getFacilityLevels =async (req:Request, res:Response, next:NextFunction) => {
    
}

export const createFacilityLevel =async (req:Request, res:Response, next:NextFunction) => {
    
}